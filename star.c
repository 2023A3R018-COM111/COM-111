#include <stdio.h>
int main()
{
    int rows,i,j,space;
    printf("enter the number of rows:");
    scanf("%d",&rows);
    for(i=1;1<=rows;i++)
    {
        for (space=1;space<=rows-i;space++)
            {
                printf(");
            }
            for(j=1;j<=2*;-i;j++)
            {
                printf("*");
            }
            printf("\n");
            }
    return 0;
}
